{
  "name": "typescript-cleanup-definitions",
  "version": "1.0.0",
  "description": "Typescript plugin to clean up desired type of definitions inside from declaration files inside a typescript project",
  "main": "./out/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "publishConfig": {
    "main":"./out/index.js"
  },
  "keywords": ["Typescript Plugin","Clean up typescript definitions"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.9.4"
  }
}
